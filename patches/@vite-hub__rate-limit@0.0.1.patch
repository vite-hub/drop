diff --git a/dist/vite.js b/dist/vite.js
index d11d255bbcf5b73f4281ac4a36574a261b238e1c..2ab8c9312bbebd25ca9f00038979f8dad70f08cc 100644
--- a/dist/vite.js
+++ b/dist/vite.js
@@ -3400,7 +3400,7 @@ function declarationError(file, source, node, message) {
 }
 function requireRateLimitBindings(program) {
 	const bindings = {
-		direct: /* @__PURE__ */ new Set(),
+		direct: /* @__PURE__ */ new Set(["requireRateLimit"]),
 		namespaces: /* @__PURE__ */ new Set()
 	};
 	for (const statement of program.body) {
@@ -3415,8 +3415,8 @@ function requireRateLimitBindings(program) {
 function resolveRateLimitBinding(name, kind, scopes) {
 	for (let index = scopes.length - 1; index >= 0; index -= 1) {
 		const scope = scopes[index];
-		if (scope[kind].has(name)) return true;
 		if (scope.shadows.has(name)) return false;
+		if (scope[kind].has(name)) return true;
 	}
 	return false;
 }
@@ -3562,7 +3562,7 @@ function extractRateLimitDeclarations(file, source) {
 	const declarations = [];
 	const scopes = [{
 		...bindings,
-		shadows: /* @__PURE__ */ new Set()
+		shadows: blockBindings({ body: parsed.program.body })
 	}];
 	addDynamicRateLimitBindingsFromStatements(parsed.program.body, scopes[0]);
 	const enterScope = (shadows) => {
