diff --git a/README.md b/README.md
index 8bb1a16289f45679f73f9537f61455681aae49a7..1a9b64183b8b8f0d5b3cb98bc4587ff69a6db310 100644
--- a/README.md
+++ b/README.md
@@ -24,7 +24,7 @@ export default defineConfig({
 
 The public presets are `cloudflare`, `netlify`, `vercel`, `deno`, and `node`. Each resolves once to a host, runtime, Nitro output, packaging policy, and service adapters; do not also set `nitro.preset`, `NITRO_PRESET`, `SERVER_PRESET`, or `VITEHUB_HOSTING`.
 
-ViteHub derives one deployment identity from the nearest `package.json` name, falling back to the Vite root directory name. Set `name` to pin the identity explicitly. `VITEHUB_DEPLOYMENT_NAME` remains a compatibility input, and conflicts with an explicit `name` fail configuration. Cloudflare uses this identity for the default Worker, Blob bucket, Queue prefix, Rate Limit namespace, and Sandbox name; explicit Worker, Blob bucket, driver, and store options still win.
+ViteHub derives one deployment identity from the nearest `package.json` name, falling back to the Vite root directory name. Set `name` to pin the identity explicitly. `VITEHUB_DEPLOYMENT_NAME` remains a compatibility input, and conflicts with an explicit `name` fail configuration. Cloudflare Workers Builds uses its connected Worker name as the default identity; an explicit ViteHub identity that differs from that target fails configuration. Cloudflare uses this identity for the default Worker, Blob bucket, Queue prefix, Rate Limit namespace, and Sandbox name; explicit Worker, Blob bucket, driver, and store options still win.
 
 Blob, Agent, Auth, Database, Email, KV, Queue, Rate Limit, Sandbox, Schedule, Workflow, and Workspace are opt-in:
 
diff --git a/dist/src-BWw6N4Sr.js b/dist/src-BWw6N4Sr.js
index 4b04dd1b8bbae40957b029c40aca6dcaec3cd3c0..0140b71e0eaca3a2be819342877601065932df41 100644
--- a/dist/src-BWw6N4Sr.js
+++ b/dist/src-BWw6N4Sr.js
@@ -843,13 +843,17 @@ function packageName(root) {
 		directory = parent;
 	}
 }
-function resolveDeploymentIdentity(root, configuredName) {
+function resolveDeploymentIdentity(root, configuredName, providerName) {
 	const configured = normalizeDeploymentName(configuredName);
 	if (configuredName !== void 0 && !configured) throw new Error("[vitehub] vitehub name must contain at least one letter or number.");
 	const environmentName = process.env.VITEHUB_DEPLOYMENT_NAME;
 	const environment = normalizeDeploymentName(environmentName);
 	if (environmentName?.trim() && !environment) throw new Error("[vitehub] VITEHUB_DEPLOYMENT_NAME must contain at least one letter or number.");
+	const provider = normalizeDeploymentName(providerName);
+	if (providerName?.trim() && !provider) throw new Error("[vitehub] WRANGLER_CI_OVERRIDE_NAME must contain at least one letter or number.");
 	if (configured && environment && configured !== environment) throw new Error(`[vitehub] vitehub name ${JSON.stringify(configuredName)} conflicts with VITEHUB_DEPLOYMENT_NAME=${JSON.stringify(environmentName)}.`);
+	const explicit = configured || environment;
+	if (explicit && provider && explicit !== provider) throw new Error(`[vitehub] deployment identity ${JSON.stringify(explicit)} conflicts with WRANGLER_CI_OVERRIDE_NAME=${JSON.stringify(providerName)}.`);
 	if (configured) return {
 		name: configured,
 		source: "vitehub.name"
@@ -858,6 +862,10 @@ function resolveDeploymentIdentity(root, configuredName) {
 		name: environment,
 		source: "VITEHUB_DEPLOYMENT_NAME"
 	};
+	if (provider) return {
+		name: provider,
+		source: "WRANGLER_CI_OVERRIDE_NAME"
+	};
 	const manifestName = normalizeDeploymentName(packageName(root));
 	if (manifestName) return {
 		name: manifestName,
@@ -909,8 +917,8 @@ function deploymentPlugins(plan, requestedServices, blobEnabled, services, optio
 				...cloneRecord(config.vitehub),
 				preset: plan.preset
 			};
 			for (const service of requestedServices) assertDeploymentService(plan, service);
-			const identity = resolveDeploymentIdentity(typeof config.root === "string" ? config.root : void 0, options.name);
+			const identity = resolveDeploymentIdentity(typeof config.root === "string" ? config.root : void 0, options.name, plan.preset === "cloudflare" ? process.env.WRANGLER_CI_OVERRIDE_NAME : void 0);
 			const name = identity.name;
 			if (requestedServices.includes("queue") && plan.services.queue.supported) config.queue = {
 				...cloneRecord(config.queue),
